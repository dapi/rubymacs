C-x C-x Set the mark point and move point where the mark was. Удобно скакать от кода к коду.
C-x C-space - global mark
C-S-Backspace delete whole line

ruby-mode:
C-c C-e - insert end

C-g Keyboard quit, deactive marking region
C-] Abort innermost recursive editing level and cancel the command which invoked it (abort-recursive-edit). 
C-/, Alt-Backspace undo

C-x C-o will delete all the empty lines around your current cursor position, except for one;
M-q re-aligns the current paragraph; I use it all the time when writing e-mails etc. (you might want to check out filladapt for a version that gives you a bit more smartness with indentations, lists etc.); 

M-; Комментарий в конце строки

Ruby/Rails:
C-: string->symbol
M-. искать где определна функция find-tag
M-, искать где функция использутся find-reverse-tag
M-* прягнуть обратно откуда искал

C-c d flymake display error

C-h k describe key
C-h after any prefix key will display all available shortcuts under that
  prefix.

C-h y YARI yet anoter ri

<C-S-f1>        ri-ruby-show-args
<C-f1>          ri-ruby-complete-symbol
<f1>            ri

C-c ] искать файлы по всему проекту (rinari-find-file-in-project)

emacs-rails-reloaded:
* Navigation around rails files,
  press [C-c '] or [C-c ;] (больше вариантов) to display available files.
  [C-c up] to toggle (controller|mailer) <> view.
  [C-c t] to toggle test <> implementation.


region (selection)	narrow-to-region	C-x n n
current page	narrow-to-page	C-x n p
function	narrow-to-defun	C-x n d
everything	widen	C-x n w
